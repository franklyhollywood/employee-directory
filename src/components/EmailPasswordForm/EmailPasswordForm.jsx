import React from 'react';

export default function EmailPasswordForm() {
  return (
    <div>
      email input, password input, button changes depending on signing in or
      signing up
    </div>
  );
}
