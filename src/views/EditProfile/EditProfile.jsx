export default function EditProfile() {
  return (
    <div>
      Profile information except editable, message: Need to edit or create
      profile
    </div>
  );
}

//form to create profile form
//Name string
//email: string (disabled; not editable, but still visible in the form)
//birthday
